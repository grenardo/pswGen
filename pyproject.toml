[project]
name = "pswGen"
version = "0.1.0"
description = ""
authors = [
    {name = "grenardo",email = "giampytek669@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[project.scripts]
mytool = "password_gen.core:main"

[tool.poetry]
packages = [{include = "pswGen", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
